
- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: installing the package
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
    - mysql
    - nginx
    - zip